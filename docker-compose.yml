version: "3"
services:
  client:
    image: "barisege/fibonacci-calculator-client"
    mem_limit: 128m
    hostname: client
  server:
    image: "barisege/fibonacci-calculator-server"
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: "barisege/fibonacci-calculator-worker"
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    restart: always
    image: "barisege/fibonacci-calculator-nginx"
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
